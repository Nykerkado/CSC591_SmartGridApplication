# CSC591_SmartGridApplication
Smart Grid Monitoring Application

Note after pulling run on command line ( I used Command Prompt):  
**npm install**   *this should install all the imports in the json file  
**npm run dev**    *Ctrl+Click on local host link
